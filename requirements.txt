# Default to a CUDA 12.1 PyTorch wheel because many RTX 3090 servers run
# 12.x drivers; if your driver is newer, you can replace this with the
# install command generated at https://pytorch.org/get-started/locally/.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121

# Unsloth stack for SFT + RL (GRPO). The notebook reference pins recent
# Transformers / TRL versions; keep them explicit for reproducible servers.
unsloth
unsloth_zoo
transformers==4.56.2
trl==0.22.2
huggingface_hub>=0.34.0
datasets==4.3.0
accelerate
peft
bitsandbytes
xformers
sentencepiece
protobuf
safetensors
pyyaml

# Notebook UI for running notebooks/unsloth_sft_grpo_qwen3.ipynb
notebook
ipykernel
